# colla

Small Python CLI for file and config utilities.

## Install

```bash
pip install -e .
```

## Usage

```bash
colla --help
```

## Commands

Run `colla --version` to check the installed version.
