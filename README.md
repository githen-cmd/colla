# colla

Small Python CLI for file and config helpers.

## Install

```bash
pip install -e .
```

## Usage

```bash
colla --help
```

## Commands

Run `colla --version` to check the installed version.

## Development

```bash
python -m pytest
```
